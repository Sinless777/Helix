# Template .npmrc for publishing libs from CI or local env.
# DO NOT commit your real token into the repository. Use your CI or local shell to
# create a .npmrc file from this template before publishing.
# Example (Linux/macOS):
#   echo "//registry.npmjs.org/:_authToken=${NPM_ACCESS_TOKEN}" > .npmrc

//registry.npmjs.org/:_authToken=${NPM_ACCESS_TOKEN}
