apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-vars
  namespace: flux-system
  labels:
    app.kubernetes.io/name: cluster-vars
    app.kubernetes.io/part-of: helix-platform
    app.kubernetes.io/managed-by: fluxcd
    helix.ai/component: config
spec:
  interval: 10m0s
  retryInterval: 2m0s
  timeout: 2m0s
  prune: true
  wait: true
  path: ./IaC/kubernetes/flux/vars
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
