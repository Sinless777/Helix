---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: helix-ai
  namespace: flux-system
  labels:
    app.kubernetes.io/name: helix-ai
    app.kubernetes.io/part-of: helix-platform
    app.kubernetes.io/managed-by: fluxcd
spec:
  interval: 10m
  timeout: 2m
  url: https://github.com/Sinless777/Helix.git
  ref:
    branch: main
  # Add authentication secret if the repo is private; e.g.:
  # secretRef:
  #   name: helix-ai-auth
