apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: bjw-s
  namespace: flux-system
  labels:
    app.kubernetes.io/name: bjw-s
    app.kubernetes.io/part-of: helix-platform
    app.kubernetes.io/managed-by: fluxcd
    helix.ai/component: platform-charts
spec:
  type: oci
  interval: 10m # polling interval tuned for production stability
  url: oci://ghcr.io/bjw-s/helm
  secretRef: null # Add a secret here if the OCI registry requires auth
