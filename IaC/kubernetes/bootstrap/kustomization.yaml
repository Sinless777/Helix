apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: bootstrap
  namespace: flux-system
  labels:
    app.kubernetes.io/name: bind-sops-secret-reader
    app.kubernetes.io/part-of: helix-platform
    helix.ai/component: gitops-control-plane

resources:
  - ./namespace.yaml
  - ./rbac.yaml
  - ./sops-age.yaml
  - ./00-crds
