apiVersion: v1
kind: Namespace
metadata:
  name: networking
  labels:
    app.kubernetes.io/name: networking
    app.kubernetes.io/part-of: helix-platform
    app.kubernetes.io/managed-by: fluxcd
    helix.ai/component: networking
  annotations:
    kubernetes.io/description: "Platform networking namespace for Helix system (service mesh, ingress, DNS, etc.)"