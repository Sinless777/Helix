---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dnsendpoints.externaldns.k8s.io
  labels:
    app.kubernetes.io/name: dnsendpoints
    app.kubernetes.io/part-of: helix-platform
    app.kubernetes.io/managed-by: fluxcd
    helix.ai/component: dns-automation
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
    api-approved.kubernetes.io: "https://github.com/kubernetes-sigs/external-dns/pull/2007"
spec:
  group: externaldns.k8s.io
  scope: Namespaced
  names:
    kind: DNSEndpoint
    listKind: DNSEndpointList
    plural: dnsendpoints
    singular: dnsendpoint
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: DNSEndpointSpec defines the desired state of the DNS record endpoints.
              type: object
              properties:
                endpoints:
                  description: A list of DNS record endpoints to manage.
                  type: array
                  items:
                    type: object
                    properties:
                      dnsName:
                        description: The hostname of the DNS record.
                        type: string
                      recordType:
                        description: The type of DNS record, e.g. A, CNAME, TXT.
                        type: string
                      recordTTL:
                        description: TTL for the record in seconds.
                        format: int64
                        type: integer
                      targets:
                        description: The targets the DNS record points to.
                        type: array
                        items:
                          type: string
                      labels:
                        description: Labels stores custom labels defined for the endpoint.
                        type: object
                        additionalProperties:
                          type: string
                      providerSpecific:
                        description: ProviderSpecific stores provider specific config.
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            value:
                              type: string
            status:
              description: DNSEndpointStatus defines the observed state of a DNSEndpoint.
              type: object
              properties:
                observedGeneration:
                  description: The generation observed by the external dns controller.
                  format: int64
                  type: integer
      subresources:
        status: {}
status:
  storedVersions:
    - v1alpha1
