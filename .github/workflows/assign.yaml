# .github/workflows/assign.yaml
name: 👥 Auto-assign, label & milestone (issues & PRs)

on:
  workflow_dispatch:
  issues:
    types: [opened, edited, labeled, reopened]
  pull_request_target:
    types: [opened, ready_for_review, reopened, synchronize, edited, labeled]

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: auto-assign-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  assign:
    name: Process issue/PR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.base.ref || github.ref_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install YAML parser
        run: npm install -g yaml @actions/core

      - name: Apply assignment rules
        run: ./.github/scripts/assign.js
        env:
          CONFIG_FILE: .github/assignees.yaml
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}