#

## files

```zsh
libs/core/src/lib/constants/types/
│
├── api.ts                # Shared API request/response, pagination, error, status
├── auth.ts               # User, session, JWT, OAuth, permissions, roles
├── card.ts               # UI Card & ListItemProps (you already created this)
├── citation.ts           # APA citation, citation page data
├── config.ts             # System configs, feature flags, environment variables
├── data.ts               # Database entity types, DTOs, storage metadata
├── developer.ts          # Git flow nodes/edges, commit conventions, PR process
├── docs.ts               # Documentation navigation, sections, page metadata
├── events.ts             # Event bus types, system events, audit events
├── form.ts               # Form state, validation, inputs, errors
├── navigation.ts         # NavItem, NavSection, sidebar/tree structure
├── org.ts                # Organization, workspace, membership types\
├── system.ts             # System colors, CLI styles, logging, enums
├── technology.ts         # Tech stack categories (AI, Cloud, Networking, etc.)
├── theme.ts              # Theme, palette, typography, design tokens
└── index.ts              # Barrel export for all the above

```
