import type { Theme } from '@mui/material/styles'

export { default as DocsTheme } from './docs'
export { default as MainTheme } from './main'

// Optionally, type guard for consumers
export type { Theme }
