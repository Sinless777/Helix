/* eslint-disable */
// biome-ignore-all lint: <auto-generated-file>
// This file was automatically generated. Do not edit manually.

import * as sdk from "hypertune";

export const queryId = "fbacfaa8-e5ab-526d-89f6-02e5a6a90d65";

export const query: sdk.Query<sdk.ObjectValueWithVariables> = {"variableDefinitions":{},"fragmentDefinitions":{},"fieldQuery":{"Query":{"type":"InlineFragment","objectTypeName":"Query","selection":{"root":{"fieldArguments":{"__isPartialObject__":true},"fieldQuery":{"Root":{"type":"InlineFragment","objectTypeName":"Root","selection":{"waitlistEnabled":{"fieldArguments":{},"fieldQuery":null},"permissionsSystem":{"fieldArguments":{},"fieldQuery":null},"sandboxMode":{"fieldArguments":{},"fieldQuery":null},"orgAdminMode":{"fieldArguments":{},"fieldQuery":null},"developerToolsEnabled":{"fieldArguments":{},"fieldQuery":null},"darkeThemeDefault":{"fieldArguments":{},"fieldQuery":null},"dashboardModeEnabled":{"fieldArguments":{},"fieldQuery":null},"pluginDrawerBeta":{"fieldArguments":{},"fieldQuery":null},"enableMarkdownToolsUI":{"fieldArguments":{},"fieldQuery":null},"metricsTracingVerbose":{"fieldArguments":{},"fieldQuery":null},"showUserDashboard":{"fieldArguments":{},"fieldQuery":null},"usageMeteringEnabled":{"fieldArguments":{},"fieldQuery":null},"telemetryEnabled":{"fieldArguments":{},"fieldQuery":null},"enforceZeroTrustPlugin":{"fieldArguments":{},"fieldQuery":null},"disableOpenaiFallback":{"fieldArguments":{},"fieldQuery":null},"dataRetentionOverride":{"fieldArguments":{},"fieldQuery":null},"modelExperiments":{"fieldArguments":{},"fieldQuery":null},"pluginReviewRequired":{"fieldArguments":{},"fieldQuery":null},"auditLogStrictMode":{"fieldArguments":{},"fieldQuery":null},"agentAutonomyBeta":{"fieldArguments":{},"fieldQuery":null},"webScraperEnabled":{"fieldArguments":{},"fieldQuery":null},"ebeddingSerceEnabled":{"fieldArguments":{},"fieldQuery":null},"voiceInterfaceEnabled":{"fieldArguments":{},"fieldQuery":null},"choasModeEnabled":{"fieldArguments":{},"fieldQuery":null},"tierEnterpriseFeatures":{"fieldArguments":{},"fieldQuery":null},"tierProFeatures":{"fieldArguments":{},"fieldQuery":null},"tierPremiumPlusFeatures":{"fieldArguments":{},"fieldQuery":null},"tierPremiumFeatures":{"fieldArguments":{},"fieldQuery":null},"tierBasicPlusFeatures":{"fieldArguments":{},"fieldQuery":null},"tierBasicFeatures":{"fieldArguments":{},"fieldQuery":null},"personaOverlays":{"fieldArguments":{},"fieldQuery":null},"toolsUIEnabled":{"fieldArguments":{},"fieldQuery":null},"pluginRuntimeEnabled":{"fieldArguments":{},"fieldQuery":null},"memoryV1":{"fieldArguments":{},"fieldQuery":null},"assistantEnabled":{"fieldArguments":{},"fieldQuery":null}}}}}}}}};

export const vercelFlagDefinitions = {"waitlistEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EwaitlistEnabled"},"permissionsSystem":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpermissionsSystem"},"sandboxMode":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EsandboxMode"},"orgAdminMode":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EorgAdminMode"},"developerToolsEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdeveloperToolsEnabled"},"darkeThemeDefault":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdarkeThemeDefault"},"dashboardModeEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdashboardModeEnabled"},"pluginDrawerBeta":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpluginDrawerBeta"},"enableMarkdownToolsUI":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EenableMarkdownToolsUI"},"metricsTracingVerbose":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EmetricsTracingVerbose"},"showUserDashboard":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EshowUserDashboard"},"usageMeteringEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EusageMeteringEnabled"},"telemetryEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtelemetryEnabled"},"enforceZeroTrustPlugin":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EenforceZeroTrustPlugin"},"disableOpenaiFallback":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdisableOpenaiFallback"},"dataRetentionOverride":{"options":[],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdataRetentionOverride"},"modelExperiments":{"options":[],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EmodelExperiments"},"pluginReviewRequired":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpluginReviewRequired"},"auditLogStrictMode":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EauditLogStrictMode"},"agentAutonomyBeta":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EagentAutonomyBeta"},"webScraperEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EwebScraperEnabled"},"ebeddingSerceEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EebeddingSerceEnabled"},"voiceInterfaceEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EvoiceInterfaceEnabled"},"choasModeEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EchoasModeEnabled"},"tierEnterpriseFeatures":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierEnterpriseFeatures"},"tierProFeatures":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierProFeatures"},"tierPremiumPlusFeatures":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierPremiumPlusFeatures"},"tierPremiumFeatures":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierPremiumFeatures"},"tierBasicPlusFeatures":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierBasicPlusFeatures"},"tierBasicFeatures":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierBasicFeatures"},"personaOverlays":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpersonaOverlays"},"toolsUIEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtoolsUIEnabled"},"pluginRuntimeEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpluginRuntimeEnabled"},"memoryV1":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EmemoryV1"},"assistantEnabled":{"options":[{"label":"Off","value":false},{"label":"On","value":true}],"origin":"https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EassistantEnabled"}};

export type FlagValues = {
  "waitlistEnabled": boolean;
  "permissionsSystem": boolean;
  "sandboxMode": boolean;
  "orgAdminMode": boolean;
  "developerToolsEnabled": boolean;
  "darkeThemeDefault": boolean;
  "dashboardModeEnabled": boolean;
  "pluginDrawerBeta": boolean;
  "enableMarkdownToolsUI": boolean;
  "metricsTracingVerbose": boolean;
  "showUserDashboard": boolean;
  "usageMeteringEnabled": boolean;
  "telemetryEnabled": boolean;
  "enforceZeroTrustPlugin": boolean;
  "disableOpenaiFallback": boolean;
  "dataRetentionOverride": number;
  "modelExperiments": string;
  "pluginReviewRequired": boolean;
  "auditLogStrictMode": boolean;
  "agentAutonomyBeta": boolean;
  "webScraperEnabled": boolean;
  "ebeddingSerceEnabled": boolean;
  "voiceInterfaceEnabled": boolean;
  "choasModeEnabled": boolean;
  "tierEnterpriseFeatures": boolean;
  "tierProFeatures": boolean;
  "tierPremiumPlusFeatures": boolean;
  "tierPremiumFeatures": boolean;
  "tierBasicPlusFeatures": boolean;
  "tierBasicFeatures": boolean;
  "personaOverlays": boolean;
  "toolsUIEnabled": boolean;
  "pluginRuntimeEnabled": boolean;
  "memoryV1": boolean;
  "assistantEnabled": boolean;
}

export type FlagPath = keyof FlagValues & string;

export const flagFallbacks: FlagValues = {
  "waitlistEnabled": false,
  "permissionsSystem": false,
  "sandboxMode": false,
  "orgAdminMode": false,
  "developerToolsEnabled": false,
  "darkeThemeDefault": false,
  "dashboardModeEnabled": false,
  "pluginDrawerBeta": false,
  "enableMarkdownToolsUI": false,
  "metricsTracingVerbose": false,
  "showUserDashboard": false,
  "usageMeteringEnabled": false,
  "telemetryEnabled": false,
  "enforceZeroTrustPlugin": false,
  "disableOpenaiFallback": false,
  "dataRetentionOverride": 0,
  "modelExperiments": "",
  "pluginReviewRequired": false,
  "auditLogStrictMode": false,
  "agentAutonomyBeta": false,
  "webScraperEnabled": false,
  "ebeddingSerceEnabled": false,
  "voiceInterfaceEnabled": false,
  "choasModeEnabled": false,
  "tierEnterpriseFeatures": false,
  "tierProFeatures": false,
  "tierPremiumPlusFeatures": false,
  "tierPremiumFeatures": false,
  "tierBasicPlusFeatures": false,
  "tierBasicFeatures": false,
  "personaOverlays": false,
  "toolsUIEnabled": false,
  "pluginRuntimeEnabled": false,
  "memoryV1": false,
  "assistantEnabled": false,
}

export function decodeFlagValues<TFlagPath extends keyof FlagValues & string>(
  encodedFlagValues: string,
  flagPaths: TFlagPath[]
): Pick<FlagValues, TFlagPath> {
  return sdk.decodeFlagValues({ encodedFlagValues, flagPaths })
}

export type VariableValues = {};

export type User = {
  id: string;
  name: string;
  email: string;
}

export const EnvironmentEnumValues = [
  "development",
  "production",
  "test"
] as const;
export type Environment = typeof EnvironmentEnumValues[number];

/**
 * This `Context` input type is used for the `context` argument on your root field.
 * It contains details of the current `user` and `environment`.
 * 
 * You can define other custom input types with fields that are primitives, enums 
 * or other input types.
 */
export type Context = {
  user: User;
  environment: Environment;
}

export type RootArgs = {
  context: Context;
}

export type EmptyObject = {};

export type Root = {
  waitlistEnabled: boolean;
  permissionsSystem: boolean;
  sandboxMode: boolean;
  orgAdminMode: boolean;
  developerToolsEnabled: boolean;
  darkeThemeDefault: boolean;
  dashboardModeEnabled: boolean;
  pluginDrawerBeta: boolean;
  enableMarkdownToolsUI: boolean;
  metricsTracingVerbose: boolean;
  showUserDashboard: boolean;
  usageMeteringEnabled: boolean;
  telemetryEnabled: boolean;
  enforceZeroTrustPlugin: boolean;
  disableOpenaiFallback: boolean;
  dataRetentionOverride: number;
  modelExperiments: string;
  pluginReviewRequired: boolean;
  auditLogStrictMode: boolean;
  agentAutonomyBeta: boolean;
  webScraperEnabled: boolean;
  ebeddingSerceEnabled: boolean;
  voiceInterfaceEnabled: boolean;
  choasModeEnabled: boolean;
  tierEnterpriseFeatures: boolean;
  tierProFeatures: boolean;
  tierPremiumPlusFeatures: boolean;
  tierPremiumFeatures: boolean;
  tierBasicPlusFeatures: boolean;
  tierBasicFeatures: boolean;
  personaOverlays: boolean;
  toolsUIEnabled: boolean;
  pluginRuntimeEnabled: boolean;
  memoryV1: boolean;
  assistantEnabled: boolean;
}

const rootFallback = {waitlistEnabled:false,permissionsSystem:false,sandboxMode:false,orgAdminMode:false,developerToolsEnabled:false,darkeThemeDefault:false,dashboardModeEnabled:false,pluginDrawerBeta:false,enableMarkdownToolsUI:false,metricsTracingVerbose:false,showUserDashboard:false,usageMeteringEnabled:false,telemetryEnabled:false,enforceZeroTrustPlugin:false,disableOpenaiFallback:false,dataRetentionOverride:0,modelExperiments:"",pluginReviewRequired:false,auditLogStrictMode:false,agentAutonomyBeta:false,webScraperEnabled:false,ebeddingSerceEnabled:false,voiceInterfaceEnabled:false,choasModeEnabled:false,tierEnterpriseFeatures:false,tierProFeatures:false,tierPremiumPlusFeatures:false,tierPremiumFeatures:false,tierBasicPlusFeatures:false,tierBasicFeatures:false,personaOverlays:false,toolsUIEnabled:false,pluginRuntimeEnabled:false,memoryV1:false,assistantEnabled:false};

export class RootNode extends sdk.Node {
  override typeName = "Root" as const;

  getRootArgs(): RootArgs {
    const { step } = this.props;
    return (step?.type === 'GetFieldStep' ? step.fieldArguments : {}) as RootArgs;
  }

  get({ fallback = rootFallback as Root}: { fallback?: Root } = {}): Root {
    const getQuery = sdk.mergeFieldQueryAndArgs(
      query.fragmentDefinitions,
      sdk.getFieldQueryForPath(query.fragmentDefinitions, query.fieldQuery, ["Query", "root"]), 
      null,
    );
    return this.getValue({ query: getQuery, fallback }) as Root;
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EwaitlistEnabled})
   */
  waitlistEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("waitlistEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpermissionsSystem})
   */
  permissionsSystem({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("permissionsSystem", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EsandboxMode})
   */
  sandboxMode({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("sandboxMode", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EorgAdminMode})
   */
  orgAdminMode({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("orgAdminMode", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdeveloperToolsEnabled})
   */
  developerToolsEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("developerToolsEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdarkeThemeDefault})
   */
  darkeThemeDefault({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("darkeThemeDefault", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdashboardModeEnabled})
   */
  dashboardModeEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("dashboardModeEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpluginDrawerBeta})
   */
  pluginDrawerBeta({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("pluginDrawerBeta", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EenableMarkdownToolsUI})
   */
  enableMarkdownToolsUI({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("enableMarkdownToolsUI", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EmetricsTracingVerbose})
   */
  metricsTracingVerbose({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("metricsTracingVerbose", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EshowUserDashboard})
   */
  showUserDashboard({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("showUserDashboard", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EusageMeteringEnabled})
   */
  usageMeteringEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("usageMeteringEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtelemetryEnabled})
   */
  telemetryEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("telemetryEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EenforceZeroTrustPlugin})
   */
  enforceZeroTrustPlugin({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("enforceZeroTrustPlugin", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdisableOpenaiFallback})
   */
  disableOpenaiFallback({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("disableOpenaiFallback", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EdataRetentionOverride})
   */
  dataRetentionOverride({ args = {}, fallback }: { args?: EmptyObject; fallback: number; }): number {
    const props0 = this.getFieldNodeProps("dataRetentionOverride", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "IntExpression"
    ) {
      const node = new sdk.IntNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.IntNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EmodelExperiments})
   */
  modelExperiments({ args = {}, fallback }: { args?: EmptyObject; fallback: string; }): string {
    const props0 = this.getFieldNodeProps("modelExperiments", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "StringExpression"
    ) {
      const node = new sdk.StringNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.StringNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpluginReviewRequired})
   */
  pluginReviewRequired({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("pluginReviewRequired", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EauditLogStrictMode})
   */
  auditLogStrictMode({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("auditLogStrictMode", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EagentAutonomyBeta})
   */
  agentAutonomyBeta({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("agentAutonomyBeta", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EwebScraperEnabled})
   */
  webScraperEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("webScraperEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EebeddingSerceEnabled})
   */
  ebeddingSerceEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("ebeddingSerceEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EvoiceInterfaceEnabled})
   */
  voiceInterfaceEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("voiceInterfaceEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EchoasModeEnabled})
   */
  choasModeEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("choasModeEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierEnterpriseFeatures})
   */
  tierEnterpriseFeatures({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("tierEnterpriseFeatures", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierProFeatures})
   */
  tierProFeatures({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("tierProFeatures", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierPremiumPlusFeatures})
   */
  tierPremiumPlusFeatures({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("tierPremiumPlusFeatures", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierPremiumFeatures})
   */
  tierPremiumFeatures({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("tierPremiumFeatures", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierBasicPlusFeatures})
   */
  tierBasicPlusFeatures({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("tierBasicPlusFeatures", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtierBasicFeatures})
   */
  tierBasicFeatures({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("tierBasicFeatures", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpersonaOverlays})
   */
  personaOverlays({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("personaOverlays", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EtoolsUIEnabled})
   */
  toolsUIEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("toolsUIEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EpluginRuntimeEnabled})
   */
  pluginRuntimeEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("pluginRuntimeEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EmemoryV1})
   */
  memoryV1({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("memoryV1", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }

  /**
   * [Open in Hypertune UI]({@link https://app.hypertune.com/projects/6999/main/draft/logic?selected_field_path=root%3EassistantEnabled})
   */
  assistantEnabled({ args = {}, fallback }: { args?: EmptyObject; fallback: boolean; }): boolean {
    const props0 = this.getFieldNodeProps("assistantEnabled", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "BooleanExpression"
    ) {
      const node = new sdk.BooleanNode(props0);
      return node.get({ fallback });
    }

    const node = new sdk.BooleanNode(props0);
    node._logUnexpectedTypeError();
    return node.get({ fallback });
  }
}

/**
 * This is your project schema expressed in GraphQL.
 * 
 * Define `Boolean` fields for feature flags, custom `enum` fields for flags with 
 * more than two states, `Int` fields for numeric flags like timeouts and limits, 
 * `String` fields to manage in-app copy, `Void` fields for analytics events, and 
 * fields with custom object and list types for more complex app configuration, 
 * e.g. to use Hypertune as a CMS.
 * 
 * Once you've changed your schema, set your flag logic in the Logic view.
 */
export type Source = {
  /**
   * You can add arguments to any field in your schema, which you can then use when
   * setting its logic, including the logic of any nested fields. Your root field 
   * already has a `context` argument. Since all flags are nested under the root 
   * field, this context will be available to all of them.
   */
  root: Root;
}

const sourceFallback = {root:{waitlistEnabled:false,permissionsSystem:false,sandboxMode:false,orgAdminMode:false,developerToolsEnabled:false,darkeThemeDefault:false,dashboardModeEnabled:false,pluginDrawerBeta:false,enableMarkdownToolsUI:false,metricsTracingVerbose:false,showUserDashboard:false,usageMeteringEnabled:false,telemetryEnabled:false,enforceZeroTrustPlugin:false,disableOpenaiFallback:false,dataRetentionOverride:0,modelExperiments:"",pluginReviewRequired:false,auditLogStrictMode:false,agentAutonomyBeta:false,webScraperEnabled:false,ebeddingSerceEnabled:false,voiceInterfaceEnabled:false,choasModeEnabled:false,tierEnterpriseFeatures:false,tierProFeatures:false,tierPremiumPlusFeatures:false,tierPremiumFeatures:false,tierBasicPlusFeatures:false,tierBasicFeatures:false,personaOverlays:false,toolsUIEnabled:false,pluginRuntimeEnabled:false,memoryV1:false,assistantEnabled:false}};

export type GetQueryRootArgs = {
  args: RootArgs;
}

export type GetQueryArgs = {
  root: GetQueryRootArgs;
}

/**
 * This is your project schema expressed in GraphQL.
 * 
 * Define `Boolean` fields for feature flags, custom `enum` fields for flags with 
 * more than two states, `Int` fields for numeric flags like timeouts and limits, 
 * `String` fields to manage in-app copy, `Void` fields for analytics events, and 
 * fields with custom object and list types for more complex app configuration, 
 * e.g. to use Hypertune as a CMS.
 * 
 * Once you've changed your schema, set your flag logic in the Logic view.
 */
export class SourceNode extends sdk.Node {
  override typeName = "Query" as const;

  get({ args, fallback = sourceFallback as Source}: { args: GetQueryArgs; fallback?: Source }): Source {
    const getQuery = sdk.mergeFieldQueryAndArgs(
      query.fragmentDefinitions,
      sdk.getFieldQueryForPath(query.fragmentDefinitions, query.fieldQuery, []), 
      args,
    );
    return this.getValue({ query: getQuery, fallback }) as Source;
  }

  /**
   * You can add arguments to any field in your schema, which you can then use when
   * setting its logic, including the logic of any nested fields. Your root field 
   * already has a `context` argument. Since all flags are nested under the root 
   * field, this context will be available to all of them.
   */
  root({ args }: { args: RootArgs; }): RootNode {
    const props0 = this.getFieldNodeProps("root", { fieldArguments: args });
    const expression0 = props0.expression;

    if (
      expression0 &&
      expression0.type === "ObjectExpression" &&
      expression0.objectTypeName === "Root"
    ) {
      return new RootNode(props0);
    }

    const node = new RootNode(props0);
    node._logUnexpectedTypeError();
    return node;
  }
}

export type DehydratedState = sdk.DehydratedState<Source, VariableValues>

const sources: { [key: string]: SourceNode } = {};

export type CreateSourceOptions = {
  token: string; 
  variableValues?: VariableValues;
  override?: sdk.DeepPartial<Source> | null;
  key?: string;
} & sdk.CreateOptions

export function createSource({
  token,
  variableValues = {},
  override,
  key,
  ...options
}: CreateSourceOptions): SourceNode {
  const sourceKey =
    key ?? (typeof window === "undefined" ? "server" : "client");

  if (!sources[sourceKey]) {
    sources[sourceKey] = sdk.create({
      NodeConstructor: SourceNode,
      token,
      variableValues,
      override,
      options,
    });
  }

  return sources[sourceKey];
}

export const emptySource = new SourceNode({
  context: null,
  logger: null,
  parent: null,
  step: null,
  expression: null,
  initDataHash: null,
});

export function createSourceForServerOnly({
  token,
  variableValues = {},
  override,
  key,
  ...options
}: CreateSourceOptions): SourceNode {
  return typeof window === "undefined"
    ? createSource({ token, variableValues, override, ...options })
    : emptySource;
}

export const overrideCookieName = "hypertuneOverride";

/**
 * @deprecated use createSource instead.
 */
export const initHypertune = createSource
/**
 * @deprecated use SourceNode instead.
 */
export type QueryNode = SourceNode;
/**
 * @deprecated use Source instead.
 */
export type Query = Source;
