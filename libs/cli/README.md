# cls

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cls` to build the library.

## Running unit tests

Run `nx test cls` to execute the unit tests via [Jest](https://jestjs.io).
